# Copy to .env and fill in. Loaded automatically by docker compose.
# Anthropic API key for the orchestrator.
ANTHROPIC_API_KEY=
# Hugging Face token. Required only when running the `embedder` profile
# (gated model google/embeddinggemma-300m).
HF_TOKEN=

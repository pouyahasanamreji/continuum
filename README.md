# continuum

Turborepo monorepo. `apps/api` (NestJS 11), `apps/web` (Astro 6 + shadcn/ui).

## Prerequisites

- Node `>=22.12.0`
- pnpm `>=10.33`

## Scripts

- `pnpm dev` — run all apps
- `pnpm build` — build all apps
- `pnpm lint` — lint all packages
- `pnpm check-types` — typecheck all packages
